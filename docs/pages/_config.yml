# Local Jekyll preview only (cd docs/pages && jekyll serve).
# GitHub Pages uses the repo-root _config.yml (jekyll-build-pages source=.).
title: php-compiler
description: Compile PHP to native binaries — VM, LLVM JIT, AOT web deployment, and self-host bootstrap
url: "https://purhur.github.io"
baseurl: "/php-compiler"
markdown: kramdown
kramdown:
  input: GFM
exclude:
  - PAGES.md
