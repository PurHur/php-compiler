#!/usr/bin/env bash
# Repository defaults for safe CI and spawned bin/vm.php children (issue #497).
# Sourced by CI scripts, Docker wrappers, and phpc test. Override via environment.

export PHP_COMPILER_CI_RAM_GB="${PHP_COMPILER_CI_RAM_GB:-8}"
export PHP_COMPILER_MEMORY_LIMIT="${PHP_COMPILER_MEMORY_LIMIT:-1536M}"
export PHP_COMPILER_LLVM_MEMORY_LIMIT="${PHP_COMPILER_LLVM_MEMORY_LIMIT:-4096M}"
export PHP_COMPILER_DOCKER_MEM="${PHP_COMPILER_DOCKER_MEM:-10g}"
export PHP_COMPILER_DOCKER_MEM_SWAP="${PHP_COMPILER_DOCKER_MEM_SWAP:-10g}"
export PHP_COMPILER_VM_PEAK_RSS_MB="${PHP_COMPILER_VM_PEAK_RSS_MB:-2048}"
export PHP_COMPILER_VM_RSS_GUARD="${PHP_COMPILER_VM_RSS_GUARD:-1}"
export MINIWEBAPP_SERVE_GATE="${MINIWEBAPP_SERVE_GATE:-1}"
export MINIWEBAPP_WEB_SMOKE_GATE="${MINIWEBAPP_WEB_SMOKE_GATE:-1}"
export MINIWEBAPP_WEB_SMOKE_AOT_GATE="${MINIWEBAPP_WEB_SMOKE_AOT_GATE:-1}"  # default on (#1523, #833)
export SESSIONS_WEB_SMOKE_GATE="${SESSIONS_WEB_SMOKE_GATE:-1}"  # default on (#1887); 005-SessionsWeb cookie curls
export SESSIONS_WEB_SERVE_AOT_SMOKE_GATE="${SESSIONS_WEB_SERVE_AOT_SMOKE_GATE:-1}"  # default on (#2333, #2371); 005 phpc serve --aot session flash; set 0 to skip
export SESSIONS_WEB_AOT_LINK_GATE="${SESSIONS_WEB_AOT_LINK_GATE:-1}"  # default on (#1946); set 0 to skip 005 link during iteration
export SESSIONS_WEB_AOT_SMOKE_GATE="${SESSIONS_WEB_AOT_SMOKE_GATE:-0}"  # opt-in (#1891); flip to 1 when stable (#1923)
export MINIWEBAPP_AOT_LINK_GATE="${MINIWEBAPP_AOT_LINK_GATE:-1}"
export MINIWEBAPP_AOT_EXECUTE_GATE="${MINIWEBAPP_AOT_EXECUTE_GATE:-1}"  # default on (#747)
export MINIWEBAPP_JIT_PROJECT_GATE="${MINIWEBAPP_JIT_PROJECT_GATE:-1}"  # default-on in ci-fast (#730, #587); set 0 to skip
export MINIWEBAPP_SERVE_AOT_GATE="${MINIWEBAPP_SERVE_AOT_GATE:-1}"  # default on (#1524, #1067)
export SERVE_JIT_SMOKE_GATE="${SERVE_JIT_SMOKE_GATE:-0}"  # opt-in (#2274); phpc serve --jit curls 001 (+ 003 when lint green)
export EXAMPLES_SELFHOSTPROBE_SMOKE_GATE="${EXAMPLES_SELFHOSTPROBE_SMOKE_GATE:-1}"  # default-on (#2343); 008-SelfHostProbe VM lint+run (#2302, #2240)
export SELFHOSTPROBE_AOT_SMOKE_GATE="${SELFHOSTPROBE_AOT_SMOKE_GATE:-1}"  # default-on (#2407); 008 AOT build+execute via examples-aot-smoke
export EXAMPLES_AOT_SMOKE_GATE="${EXAMPLES_AOT_SMOKE_GATE:-1}"
export DEPLOY_SMOKE_GATE="${DEPLOY_SMOKE_GATE:-1}"
export DEPLOY_SMOKE_003_EXECUTE="${DEPLOY_SMOKE_003_EXECUTE:-1}"  # default on (#1530)
export SESSIONS_WEB_DEPLOY_SMOKE_GATE="${SESSIONS_WEB_DEPLOY_SMOKE_GATE:-0}"  # opt-in (#1893); 005 deploy + PHPC_DEPLOY_ROOT session flash
export FILE_UPLOAD_WEB_DEPLOY_SMOKE_GATE="${FILE_UPLOAD_WEB_DEPLOY_SMOKE_GATE:-0}"  # opt-in (#2028); 006 deploy + PHPC_DEPLOY_ROOT multipart upload
export THROWSWEB_DEPLOY_SMOKE_GATE="${THROWSWEB_DEPLOY_SMOKE_GATE:-1}"  # default-on (#2388, #2124); 007 deploy + PHPC_DEPLOY_ROOT caught invalid POST CGI; set 0 to skip
export FILE_UPLOAD_WEB_SMOKE_GATE="${FILE_UPLOAD_WEB_SMOKE_GATE:-1}"  # default on (#2009); 006 multipart curl via examples-web-smoke
export FILE_UPLOAD_WEB_SERVE_AOT_SMOKE_GATE="${FILE_UPLOAD_WEB_SERVE_AOT_SMOKE_GATE:-1}"  # default on (#2333, #2371); 006 phpc serve --aot multipart POST; set 0 to skip
export THROWS_WEB_SMOKE_GATE="${THROWS_WEB_SMOKE_GATE:-1}"  # default-on (#2125); 007-ThrowsWeb caught invalid POST curls (#2076, #2093)
export THROWSWEB_SERVE_AOT_SMOKE_GATE="${THROWSWEB_SERVE_AOT_SMOKE_GATE:-1}"  # default on (#2390, #2387); 007 phpc serve --aot caught invalid POST; set 0 to skip
export THROWSWEB_SERVE_JIT_SMOKE_GATE="${THROWSWEB_SERVE_JIT_SMOKE_GATE:-1}"  # default on (#2435, #2408); 007 phpc serve --jit caught invalid POST; set 0 to skip
export THROWSWEB_UNCAUGHT_500_GATE="${THROWSWEB_UNCAUGHT_500_GATE:-0}"  # opt-in (#2200); 007 uncaught.php HTTP 500 serve curl (#152)
export THROWSWEB_AOT_LINK_GATE="${THROWSWEB_AOT_LINK_GATE:-1}"  # default on (#2135, #2101); set 0 to skip 007 link during iteration
export THROWSWEB_AOT_SMOKE_GATE="${THROWSWEB_AOT_SMOKE_GATE:-1}"  # default on (#2135, #2101); 007 AOT CGI invalid POST execute
export FILE_UPLOAD_WEB_AOT_LINK_GATE="${FILE_UPLOAD_WEB_AOT_LINK_GATE:-1}"  # default on (#2011); set 0 to skip 006 link during iteration
export FILE_UPLOAD_WEB_AOT_SMOKE_GATE="${FILE_UPLOAD_WEB_AOT_SMOKE_GATE:-1}"  # default on (#2012); 006 AOT CGI multipart execute
export FASTCGI_SMOKE_GATE="${FASTCGI_SMOKE_GATE:-0}"  # opt-in (#173, #1899); PHPUnit FastCgi* TCP adapter (bin/fcgi.php)
export FASTCGI_WEB_SMOKE_GATE="${FASTCGI_WEB_SMOKE_GATE:-1}"  # default on (#2351, #2369); 009 VM serve health + PATH_INFO curls; set 0 to skip
export FASTCGI_WEB_AOT_SMOKE_GATE="${FASTCGI_WEB_AOT_SMOKE_GATE:-1}"  # default on (#2352, #2369); 009 AOT CGI health + PATH_INFO execute; set 0 to skip
export FASTCGI_WEB_DEPLOY_SMOKE_GATE="${FASTCGI_WEB_DEPLOY_SMOKE_GATE:-0}"  # opt-in (#2359); 009 deploy + PHPC_DEPLOY_ROOT health + PATH_INFO CGI
export BOOTSTRAP_SELFHOST_PROBE_UPDATE="${BOOTSTRAP_SELFHOST_PROBE_UPDATE:-0}"
export BOOTSTRAP_LOOP_PROBE_GATE="${BOOTSTRAP_LOOP_PROBE_GATE:-0}"
export BOOTSTRAP_M4_LOOP_PROBE="${BOOTSTRAP_M4_LOOP_PROBE:-1}"  # default on (#2780); ci-local LLVM tail after M3 strict; set 0 to skip
export BOOTSTRAP_M4_FULL_SPINE_PROBE_GATE="${BOOTSTRAP_M4_FULL_SPINE_PROBE_GATE:-0}"  # opt-in (#2770); inventory-scale gen-1 emit after loop probe; heavy (~5m)
export BOOTSTRAP_TEST_SUBSET_GATE="${BOOTSTRAP_TEST_SUBSET_GATE:-0}"  # opt-in (#2069); phpc test --bootstrap hook in ci-fast
export BOOTSTRAP_TEST_SUBSET_STRICT="${BOOTSTRAP_TEST_SUBSET_STRICT:-0}"  # opt-in strict M3 tail when subset gate on
export NORTH_STAR2_VERIFY_GATE="${NORTH_STAR2_VERIFY_GATE:-1}"  # default on in ci-fast (#1928, #2051); opt-out NORTH_STAR2_VERIFY_GATE=0
export NORTH_STAR2_THROWSWEB_GATE="${NORTH_STAR2_THROWSWEB_GATE:-1}"  # default on (#2177); 007 ladder in north-star2-verify; opt-out NORTH_STAR2_THROWSWEB_GATE=0
export NORTH_STAR3_VERIFY_GATE="${NORTH_STAR3_VERIFY_GATE:-0}"  # opt-in (#2396); ci-fast runs make north-star3-verify when 1 (#2360); default-on follow-up after probes stable
export NORTH_STAR4_VERIFY_GATE="${NORTH_STAR4_VERIFY_GATE:-0}"  # opt-in (#2429); ci-local LLVM tail runs north-star4-verify.sh when 1 (#2379)
export NORTH_STAR5_VERIFY_FAST_GATE="${NORTH_STAR5_VERIFY_FAST_GATE:-1}"  # default on (#1492); ci-fast runs north-star5-verify.sh --fast (~1–2 min); opt-out =0
export NORTH_STAR5_VERIFY_STRICT_GATE="${NORTH_STAR5_VERIFY_STRICT_GATE:-0}"  # opt-in; full M5 ladder ~1h — never default-on in CI
export BOOTSTRAP_INVENTORY_MINIMAL_SIDECARS="${BOOTSTRAP_INVENTORY_MINIMAL_SIDECARS:-1}"  # default on (#1492); argv-only sidecars at inventory link (~20s not ~6h)
export PHP_COMPILER_M3_INVENTORY_MINIMAL_SIDECARS="${PHP_COMPILER_M3_INVENTORY_MINIMAL_SIDECARS:-1}"
export PHP_COMPILER_M3_REUSE_STALE_COMPILER_LIB_SIDECAR="${PHP_COMPILER_M3_REUSE_STALE_COMPILER_LIB_SIDECAR:-1}"  # skip 2787-file spine host-compile unless forced
export BOOTSTRAP_M3_HELLOWORLD_STRICT_GATE="${BOOTSTRAP_M3_HELLOWORLD_STRICT_GATE:-1}"  # default on (#1866, #1526); opt-out BOOTSTRAP_M3_HELLOWORLD_STRICT_GATE=0
export BOOTSTRAP_M4_GEN2_STRICT_GATE="${BOOTSTRAP_M4_GEN2_STRICT_GATE:-1}"  # default on (#2112, #2075); ci-local LLVM tail runs bootstrap-loop-gen1-link.sh with BOOTSTRAP_M4_GEN2_STRICT=1; opt-out BOOTSTRAP_M4_GEN2_STRICT_GATE=0
export BOOTSTRAP_LIB_SPINE_VM_SMOKE_GATE="${BOOTSTRAP_LIB_SPINE_VM_SMOKE_GATE:-1}"  # default on (#1867); opt-out for spine-only link PRs
export BOOTSTRAP_VM_DRIVER_EXECUTE_GATE="${BOOTSTRAP_VM_DRIVER_EXECUTE_GATE:-1}"  # default on (#2227, #2201); spine bin/vm.php run() execute; opt-out BOOTSTRAP_VM_DRIVER_EXECUTE_GATE=0
export COMPILER_DRIVER_SMOKE_GATE="${COMPILER_DRIVER_SMOKE_GATE:-1}"  # default on (#2136 probe, #2137 wiring, #2168); set 0 to skip M3 driver smoke in ci-local LLVM tail
export BOOTSTRAP_COMPILER_UNIT_PROBE_GATE="${BOOTSTRAP_COMPILER_UNIT_PROBE_GATE:-1}"  # default on (#2216); ci-local LLVM tail after driver smoke, before JIT unit probe; set 0 to skip during LLVM iteration
export BOOTSTRAP_JIT_UNIT_PROBE_GATE="${BOOTSTRAP_JIT_UNIT_PROBE_GATE:-0}"  # opt-in (#2332, #2361); ci-local LLVM tail after compiler unit probe, before VM probe; default-on follow-up after probe green
export BOOTSTRAP_VM_UNIT_PROBE_GATE="${BOOTSTRAP_VM_UNIT_PROBE_GATE:-0}"  # opt-in (#2354); ci-local LLVM tail after JIT unit probe; default-on follow-up #2368
export BOOTSTRAP_PARSER_UNIT_PROBE_GATE="${BOOTSTRAP_PARSER_UNIT_PROBE_GATE:-1}"  # default on (#2409, #2417, #2419); ci-local LLVM tail after VM unit probe; set 0 to skip during LLVM iteration
export BOOTSTRAP_PHPTYPES_UNIT_PROBE_GATE="${BOOTSTRAP_PHPTYPES_UNIT_PROBE_GATE:-1}"  # default on (#2430, #2433, #2436); ci-local LLVM tail after parser unit probe; set 0 to skip during LLVM iteration
export BOOTSTRAP_M3_EMIT_TU_EXECUTE_GATE="${BOOTSTRAP_M3_EMIT_TU_EXECUTE_GATE:-0}"  # opt-in (#2444); PHPUnit @group selfhost-m3-emit after unit probes; default-on after #2442 green
export CAPABILITY_SYNTAX_CHECK="${CAPABILITY_SYNTAX_CHECK:-1}"
export M2_SPINE_ISSUE_HYGIENE_GATE="${M2_SPINE_ISSUE_HYGIENE_GATE:-1}"  # default on (#1819); opt-out for bulk spine PRs
export WAVE3_ROADMAP_SYNC_GATE="${WAVE3_ROADMAP_SYNC_GATE:-1}"  # default on (#1814); opt-out WAVE3_ROADMAP_SYNC_GATE=0 for doc-only iteration
export EXAMPLES_README_SYNC_GATE="${EXAMPLES_README_SYNC_GATE:-1}"  # default on (#1822); opt-out EXAMPLES_README_SYNC_GATE=0 for doc-only iteration
export EXAMPLES_LADDER_DISCOVERY_GATE="${EXAMPLES_LADDER_DISCOVERY_GATE:-1}"  # default on (#1913); opt-out EXAMPLES_LADDER_DISCOVERY_GATE=0 for doc-only iteration
export REBUILD_EXAMPLES_005_SYNC_GATE="${REBUILD_EXAMPLES_005_SYNC_GATE:-1}"  # default on (#1953); opt-out REBUILD_EXAMPLES_005_SYNC_GATE=0 for doc-only iteration
export REBUILD_EXAMPLES_006_SYNC_GATE="${REBUILD_EXAMPLES_006_SYNC_GATE:-1}"  # default on (#2018, #2052); opt-out REBUILD_EXAMPLES_006_SYNC_GATE=0 for doc-only iteration
export REBUILD_EXAMPLES_009_SYNC_GATE="${REBUILD_EXAMPLES_009_SYNC_GATE:-1}"  # default on (#2370); opt-out REBUILD_EXAMPLES_009_SYNC_GATE=0 for doc-only iteration
export REBUILD_EXAMPLES_003_JIT_PROJECT_SYNC_GATE="${REBUILD_EXAMPLES_003_JIT_PROJECT_SYNC_GATE:-0}"  # opt-in until #2183; set 1 after project-JIT column lands
export ROOT_README_SYNC_GATE="${ROOT_README_SYNC_GATE:-1}"  # default on (#1832, #1525); opt-out ROOT_README_SYNC_GATE=0 for doc-only iteration
export ROOT_README_006_SYNC_GATE="${ROOT_README_006_SYNC_GATE:-1}"  # default on (#2017, #2052); opt-out ROOT_README_006_SYNC_GATE=0 for doc-only iteration
export ROOT_README_007_SYNC_GATE="${ROOT_README_007_SYNC_GATE:-1}"  # default on (#2094); opt-out ROOT_README_007_SYNC_GATE=0 for doc-only iteration
export ROOT_README_008_SYNC_GATE="${ROOT_README_008_SYNC_GATE:-0}"  # opt-in (#2229); flip to 1 after #2217 green
export GETTING_STARTED_SELFHOSTPROBE_SYNC_GATE="${GETTING_STARTED_SELFHOSTPROBE_SYNC_GATE:-0}"  # opt-in (#2230); flip to 1 after #2222 §6 lands
export DEVELOPMENT_STATUS_SYNC_GATE="${DEVELOPMENT_STATUS_SYNC_GATE:-1}"  # default on (#2067, #2083); opt-out DEVELOPMENT_STATUS_SYNC_GATE=0 for doc-only iteration
export DEVELOPMENT_STATUS_007_SYNC_GATE="${DEVELOPMENT_STATUS_007_SYNC_GATE:-1}"  # default-on (#2155, #2145); opt-out DEVELOPMENT_STATUS_007_SYNC_GATE=0 for doc-only iteration
export ROOT_README_009_SYNC_GATE="${ROOT_README_009_SYNC_GATE:-1}"  # default-on (#2353); opt-out ROOT_README_009_SYNC_GATE=0 for doc-only iteration
export DEVELOPMENT_STATUS_009_SYNC_GATE="${DEVELOPMENT_STATUS_009_SYNC_GATE:-1}"  # default-on (#2353); opt-out DEVELOPMENT_STATUS_009_SYNC_GATE=0 for doc-only iteration
export SELFHOST_SPINE_COUNT_SYNC_GATE="${SELFHOST_SPINE_COUNT_SYNC_GATE:-1}"  # default on (#1834); opt-out for bulk spine PRs
export SELFHOST_SPINE_COVERAGE_SYNC_GATE="${SELFHOST_SPINE_COVERAGE_SYNC_GATE:-1}"  # default on (#1955, #1945, #1922); opt-out for bulk spine path-only PRs
export SELFHOST_SPINE_DEFERRED_SYNC_GATE="${SELFHOST_SPINE_DEFERRED_SYNC_GATE:-1}"  # default on (#2202); opt-out SELFHOST_SPINE_DEFERRED_SYNC_GATE=0 for doc-only iteration
export SELFHOST_SPINE_SIDECAR_SYNC_GATE="${SELFHOST_SPINE_SIDECAR_SYNC_GATE:-1}"  # default on (#8703); opt-out BOOTSTRAP_ALLOW_STALE_SIDECAR=1 for intentional blob batch PRs
export BOOTSTRAP_SPINE_PHPCFG_PARSE_GATE="${BOOTSTRAP_SPINE_PHPCFG_PARSE_GATE:-1}"  # default on (#2575); PHPCfg parse on compiler_minimal spine; set 0 to skip
export SELFHOST_M4_GEN2_SYNC_GATE="${SELFHOST_M4_GEN2_SYNC_GATE:-1}"  # default on (#2175, #2115); opt-out SELFHOST_M4_GEN2_SYNC_GATE=0 for doc-only iteration
export BOOTSTRAP_M3_COMPILE_SMOKE_PROBE_GATE="${BOOTSTRAP_M3_COMPILE_SMOKE_PROBE_GATE:-1}"  # default on (#1937); partial Zend emit + native run
export BOOTSTRAP_M3_COMPILE_SMOKE_STRICT_GATE="${BOOTSTRAP_M3_COMPILE_SMOKE_STRICT_GATE:-1}"  # default on (#2165); set 0 to skip strict native emit in ci-local LLVM tail
export BOOTSTRAP_RUNTIME_COMPILE_SMOKE_PROBE_GATE="${BOOTSTRAP_RUNTIME_COMPILE_SMOKE_PROBE_GATE:-1}"  # default on (#2294); partial Zend emit + native run
export BOOTSTRAP_RUNTIME_COMPILE_SMOKE_STRICT_GATE="${BOOTSTRAP_RUNTIME_COMPILE_SMOKE_STRICT_GATE:-0}"  # opt-in (#2294); flip to 1 when native runtime emit lands
export BOOTSTRAP_M3_STRICT_SYNC_GATE="${BOOTSTRAP_M3_STRICT_SYNC_GATE:-0}"  # opt-in (#2176); flip default-on after #2165 strict gate green
export M3_ALLOWLIST_SYNC_GATE="${M3_ALLOWLIST_SYNC_GATE:-1}"  # default on (#1905); opt-out M3_ALLOWLIST_SYNC_GATE=0 for bulk symbol PRs
export SELFHOST_AOT_STUB_AUDIT_GATE="${SELFHOST_AOT_STUB_AUDIT_GATE:-1}"  # default on (#8720); compile-spine stub trend snapshot vs lib/JIT.php
export BOOTSTRAP_M5_DOC_SYNC_GATE="${BOOTSTRAP_M5_DOC_SYNC_GATE:-1}"  # default on (#1984); opt-out BOOTSTRAP_M5_DOC_SYNC_GATE=0 for doc-only iteration
export BOOTSTRAP_VENDOR_INVENTORY_SYNC_GATE="${BOOTSTRAP_VENDOR_INVENTORY_SYNC_GATE:-1}"  # default on (#2040, #2030); opt-out BOOTSTRAP_VENDOR_INVENTORY_SYNC_GATE=0 for vendor-only iteration
export BOOTSTRAP_M5_DRIVER_GATE="${BOOTSTRAP_M5_DRIVER_GATE:-0}"  # legacy name (kept as alias; issue #1521)
export BOOTSTRAP_M5_DRIVER_SMOKE_GATE="${BOOTSTRAP_M5_DRIVER_SMOKE_GATE:-$BOOTSTRAP_M5_DRIVER_GATE}"  # opt-in (#2769/#1521); native gen-2 compile+run via helloworld compile driver (bootstrap-selfhost-driver-smoke)
export BOOTSTRAP_M4_FULL_SPINE_PROBE_GATE="${BOOTSTRAP_M4_FULL_SPINE_PROBE_GATE:-0}"  # opt-in (#2770); make bootstrap-loop-full-spine-probe (717-unit gen-1 emit; heavy)
export BOOTSTRAP_WAVE_CHECK_VENDOR_ABSENT_GATE="${BOOTSTRAP_WAVE_CHECK_VENDOR_ABSENT_GATE:-1}"  # default on (#8712); ci-local LLVM tail runs lib-spine smoke with vendor/ absent; opt-out =0
export BOOTSTRAP_VENDOR_PRELINK_GATE="${BOOTSTRAP_VENDOR_PRELINK_GATE:-0}"  # opt-in (#1416); make bootstrap-vendor-objects when M3 vendor JIT stable
export BOOTSTRAP_VENDOR_PRELINK_SYNC_GATE="${BOOTSTRAP_VENDOR_PRELINK_SYNC_GATE:-1}"  # default on; ci-fast checks bundle/manifest sync (#1416)
export BOOTSTRAP_VENDOR_REBUILD_AUDIT="${BOOTSTRAP_VENDOR_REBUILD_AUDIT:-0}"  # opt-in (#8718); native vendor .o drift audit in north-star5-verify --fast
export BOOTSTRAP_GEN0_MANIFEST_SYNC_GATE="${BOOTSTRAP_GEN0_MANIFEST_SYNC_GATE:-1}"  # default on; gen-0 argv driver manifest sync (#8713)
export BOOTSTRAP_INVENTORY_LINT_SYNC_GATE="${BOOTSTRAP_INVENTORY_LINT_SYNC_GATE:-0}"  # opt-in (#2210); flip default-on after inventory lint snapshot stable
export BOOTSTRAP_INVENTORY_TRIAGE_SYNC_GATE="${BOOTSTRAP_INVENTORY_TRIAGE_SYNC_GATE:-1}"  # default-on (#2265, #2389); triage top-50 vs docs/bootstrap-inventory-triage-top50.json; set 0 to skip during triage iteration
export STDLIB_JIT_DEFERRED_SYNC_GATE="${STDLIB_JIT_DEFERRED_SYNC_GATE:-1}"  # default on (#2476, #2465); stdlib-jit-audit + capabilities vs script/stdlib-jit-deferred-lib.php
export DOCTOR_GATES_MATRIX_SYNC_GATE="${DOCTOR_GATES_MATRIX_SYNC_GATE:-1}"  # default on (#2380); phpc doctor --gates vs ci-defaults.env; opt-out for doctor-only edits
export DOCS_HARNESS_HYGIENE_GATE="${DOCS_HARNESS_HYGIENE_GATE:-1}"  # default on (#2485); guard docs/README against raw docker run -v bind-mount recommendations; set 0 to skip during doc-only iteration
export INIT_MINIWEBAPP_PARITY_GATE="${INIT_MINIWEBAPP_PARITY_GATE:-1}"  # default on (#2057); opt-out INIT_MINIWEBAPP_PARITY_GATE=0 for doc-only iteration
export MINIWEBAPP_LINT_ZERO_GATE="${MINIWEBAPP_LINT_ZERO_GATE:-1}"  # default on (#2078); opt-out MINIWEBAPP_LINT_ZERO_GATE=0 during lint iteration
export MINIWEBAPP_VM_OOP_GATE="${MINIWEBAPP_VM_OOP_GATE:-1}"  # default on (#2293); ci-fast + ci-local run check-miniwebapp-vm-oop.sh (#2189, #2059); set 0 to skip
export INIT_SESSIONSWEB_PARITY_GATE="${INIT_SESSIONSWEB_PARITY_GATE:-1}"  # default on (#1902); opt-out for doc-only iteration
export INIT_FILEUPLOAD_PARITY_GATE="${INIT_FILEUPLOAD_PARITY_GATE:-1}"  # default on (#2020); opt-out INIT_FILEUPLOAD_PARITY_GATE=0 for doc-only iteration
export INIT_THROWSWEB_PARITY_GATE="${INIT_THROWSWEB_PARITY_GATE:-1}"  # default on (#2127); opt-out INIT_THROWSWEB_PARITY_GATE=0 for doc-only iteration
export INIT_SELFHOSTPROBE_PARITY_GATE="${INIT_SELFHOSTPROBE_PARITY_GATE:-1}"  # default on (#2220); opt-out INIT_SELFHOSTPROBE_PARITY_GATE=0 for doc-only iteration
export INIT_FASTCGIWEB_PARITY_GATE="${INIT_FASTCGIWEB_PARITY_GATE:-1}"  # default on (#2342); examples/009-FastCGIWeb ↔ templates/init-fastcgiweb
export APIJSON_INIT_PARITY_GATE="${APIJSON_INIT_PARITY_GATE:-1}"  # default on (#2037); opt-out APIJSON_INIT_PARITY_GATE=0 for doc-only iteration
export CAPABILITIES_SESSIONSWEB_SYNC_GATE="${CAPABILITIES_SESSIONSWEB_SYNC_GATE:-1}"  # default on (#1947); opt-out CAPABILITIES_SESSIONSWEB_SYNC_GATE=0 for doc-only iteration
export CAPABILITIES_006_SYNC_GATE="${CAPABILITIES_006_SYNC_GATE:-1}"  # default on (#2019, #2068); opt-out CAPABILITIES_006_SYNC_GATE=0 for matrix-only edits
export CAPABILITIES_THROWS_SYNC_GATE="${CAPABILITIES_THROWS_SYNC_GATE:-1}"  # default on (#2156, #2144); opt-out CAPABILITIES_THROWS_SYNC_GATE=0 for matrix-only edits
export CAPABILITIES_OOP_SYNC_GATE="${CAPABILITIES_OOP_SYNC_GATE:-0}"  # opt-in (#2190); flip default-on after OOP rows stable
export NESTED_RETURN_COMPLIANCE_GATE="${NESTED_RETURN_COMPLIANCE_GATE:-1}"  # default on (#1888); NestedReturn* VM slice after #1885
export ATTRIBUTES_COMPLIANCE_GATE="${ATTRIBUTES_COMPLIANCE_GATE:-1}"  # default on (#1904); Attribute* VM slice after #1354
export REHASH_COMPLIANCE_GATE="${REHASH_COMPLIANCE_GATE:-1}"  # default on (#1956); VMTest array/* + hashtable_rehash_unset (#66)
export REHASH_JIT_COMPLIANCE_GATE="${REHASH_JIT_COMPLIANCE_GATE:-1}"  # default on (#1959); JITTest language/array_rehash_string_keys_jit when LLVM ready (#66)
export COALESCE_COMPLIANCE_GATE="${COALESCE_COMPLIANCE_GATE:-1}"  # default on (#1960); Coalesce* VM slice after #99
export JIT_VARIABLE_FUNCTION_COMPLIANCE_GATE="${JIT_VARIABLE_FUNCTION_COMPLIANCE_GATE:-1}"  # default on (#2060); VariableFunction* JIT slice after #2055
export JIT_SERVER_SUPERGLOBAL_GATE="${JIT_SERVER_SUPERGLOBAL_GATE:-1}"  # default on (#2292); JitServerSuperglobalTest (#2257, #2275); set 0 to skip
