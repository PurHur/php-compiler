# Cases that flip between runs of the SAME COMMIT on the SAME image.
#
# Quarantined cases are excluded from both sides of the compliance diff, because a flaky case in a
# baseline manufactures phantom regressions. Entry here is a BUG, not a resting place: every line
# carries a measured direction and an issue.
#
# READ THIS BEFORE ADDING A LINE. The previous contents of this file were wrong. 18 entries were
# derived from two collects taken 15 commits apart (1d57e9d37 vs d64633260), so every "flip" they
# recorded was a CHANGE, not flakiness — and six of them were real failures made invisible (#24726).
# A case only belongs here if two runs of ONE commit disagree about it.
#
# Re-measured 2026-07-29 from two complete 24-shard CI collects at a3f32662b
# (runs 30461221640 and 30461258793): 8,112 executed cases, and the two runs disagreed about
# EXACTLY ONE of them. The suite is deterministic when the tree is held constant.

# Lines may be scoped to a suite as SUITE:name. Flakiness is suite-specific: hrtime flips
# between VMTest runs of one commit, but fails STABLY under JITTest in both runs of e7de99700,
# so an unscoped entry would drop a real JITTest failure from that baseline.
VMTest:stdlib/hrtime_nanosecond_precision   # fails ~1 run in 2; nanosecond deltas under runner jitter (#24870)
