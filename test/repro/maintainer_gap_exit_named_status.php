<?php

declare(strict_types=1);

// Maintainer gap: exit(status:)/die(status:) named parameter on PHP 8.4 forward profile (#16082).
// Requires PHP_COMPILER_PROFILE=8.4 (or host PHP >= 8.4.0).
exit(status: 0);
